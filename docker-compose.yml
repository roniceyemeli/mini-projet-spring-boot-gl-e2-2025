
services:
  postgres-user:
    image: postgres:17-alpine
    container_name: postgres-user-db
    environment:
      POSTGRES_DB: user_db
      POSTGRES_USER: user_admin
      POSTGRES_PASSWORD: user_password123
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8"
    ports:
      - "5437:5432"
    volumes:
      - postgres-user-data:/var/lib/postgresql/data
      - ./init-user.sql:/docker-entrypoint-initdb.d/init-user.sql
    networks:
      - app-network
    restart: unless-stopped

  postgres-student:
    image: postgres:17-alpine
    container_name: postgres-student-db
    environment:
      POSTGRES_DB: student_db
      POSTGRES_USER: student_admin
      POSTGRES_PASSWORD: student_password123
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8"
    ports:
      - "5433:5432"
    volumes:
      - postgres-student-data:/var/lib/postgresql/data
      - ./init-student.sql:/docker-entrypoint-initdb.d/init-student.sql
    networks:
      - app-network
    restart: unless-stopped

  postgres-event:
    image: postgres:17-alpine
    container_name: postgres-event-db
    environment:
      POSTGRES_DB: event_db
      POSTGRES_USER: event_admin
      POSTGRES_PASSWORD: event_password123
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8"
    ports:
      - "5434:5432"
    volumes:
      - postgres-event-data:/var/lib/postgresql/data
      - ./init-event.sql:/docker-entrypoint-initdb.d/init-event.sql
    networks:
      - app-network
    restart: unless-stopped

  postgres-school:
    image: postgres:17-alpine
    container_name: postgres-school-db
    environment:
      POSTGRES_DB: school_db
      POSTGRES_USER: school_admin
      POSTGRES_PASSWORD: school_password123
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8"
    ports:
      - "5435:5432"
    volumes:
      - postgres-school-data:/var/lib/postgresql/data
      - ./init-school.sql:/docker-entrypoint-initdb.d/init-school.sql
    networks:
      - app-network
    restart: unless-stopped

  postgres-community:
    image: postgres:17-alpine
    container_name: postgres-community-db
    environment:
      POSTGRES_DB: community_db
      POSTGRES_USER: community_admin
      POSTGRES_PASSWORD: community_password123
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8"
    ports:
      - "5436:5432"
    volumes:
      - postgres-community-data:/var/lib/postgresql/data
      - ./init-community.sql:/docker-entrypoint-initdb.d/init-community.sql
    networks:
      - app-network
    restart: unless-stopped

#  pgadmin:
#    image: dpage/pgadmin4
#    container_name: pgadmin
#    environment:
#      PGADMIN_DEFAULT_EMAIL: admin@admin.com
#      PGADMIN_DEFAULT_PASSWORD: admin123
#    ports:
#      - "5050:80"
#    volumes:
#      - pgadmin-data:/var/lib/pgadmin
#    networks:
#      - app-network
#    depends_on:
#      - postgres-user
#      - postgres-student
#      - postgres-school
#      - postgres-community
#      - postgres-event

  adminer:
    image: adminer:latest
    container_name: adminer
    environment:
      ADMINER_DEFAULT_SERVER: postgres-user
    ports:
      - "8080:8080"
    networks:
      - app-network
    depends_on:
      - postgres-user
      - postgres-student
      - postgres-event
      - postgres-community
      - postgres-school
    restart: unless-stopped

networks:
  app-network:
    driver: bridge

volumes:
  postgres-user-data:
  postgres-student-data:
  postgres-event-data:
  postgres-school-data:
  postgres-community-data:
#  pgadmin-data: